import { useState } from 'react';
import { Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
import { router, useLocalSearchParams } from 'expo-router';
import type { Recipe } from '../../../src/entities/recipe/model/types';
import { PrimaryButton } from '../../../src/shared/ui/PrimaryButton';

export default function Checklist(){const {data}=useLocalSearchParams<{data:string}>();const recipe:Recipe=JSON.parse(String(data));const [checked,setChecked]=useState<string[]>([]);const toggle=(name:string)=>setChecked(v=>v.includes(name)?v.filter(x=>x!==name):[...v,name]);return <SafeAreaView style={s.safe}><View style={s.container}><Pressable onPress={()=>router.back()}><Text style={s.back}>← 레시피 상세</Text></Pressable><Text style={s.eyebrow}>STEP 3 · PREPARE</Text><Text style={s.title}>재료를 준비했는지 확인해주세요.</Text><View style={s.progress}><Text>준비 완료 {checked.length}/{recipe.ingredients.length}</Text><View style={s.track}><View style={[s.fill,{width:`${checked.length/recipe.ingredients.length*100}%`}]}/></View></View>{recipe.ingredients.map(i=><Pressable style={s.item} key={i.name} onPress={()=>toggle(i.name)}><Text style={s.box}>{checked.includes(i.name)?'☑':'☐'}</Text><Text style={{flex:1}}>{i.name}</Text><Text style={s.muted}>{i.amount}</Text></Pressable>)}<PrimaryButton title="조리 시작 →" onPress={()=>router.push({pathname:'/recipe/[id]/cooking',params:{id:recipe.id,data:JSON.stringify(recipe)}})}/></View></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#FFFAF4'},container:{padding:24},back:{color:'#817983',marginBottom:26},eyebrow:{fontSize:11,letterSpacing:1.5,color:'#F26D3D',fontWeight:'800',marginBottom:12},title:{fontSize:32,fontWeight:'800',lineHeight:42,marginBottom:24},progress:{padding:16,borderRadius:16,backgroundColor:'#FFF',borderWidth:1,borderColor:'#ECE6E1',marginBottom:14},track:{height:7,borderRadius:9,backgroundColor:'#F0ECE7',marginTop:12,overflow:'hidden'},fill:{height:7,borderRadius:9,backgroundColor:'#F26D3D'},item:{flexDirection:'row',alignItems:'center',gap:10,paddingVertical:15,borderBottomWidth:1,borderBottomColor:'#ECE6E1'},box:{fontSize:23},muted:{color:'#817983'}});
