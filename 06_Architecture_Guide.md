# 오늘픽 아키텍처 가이드

## 1. 현재 기준 아키텍처

오늘픽의 현재 기준 아키텍처는 다음과 같다.

```text
React Native + Expo 모바일 앱
        ↓
FastAPI 단일 API 서버
        ↓
PostgreSQL 단일 데이터베이스
```

추가 저장소와 부가 서비스는 필요 시 단계적으로 붙인다.

- 이미지 저장: Cloudflare R2 또는 S3 호환 스토리지
- 캐시: Redis
- 비동기 작업: ARQ 또는 Celery
- 오류 추적: Sentry

## 2. 이 기준을 채택하는 이유

- 현재 모바일 코드는 HTTP API 호출을 전제로 작성되어 있다.
- 앱 내부는 `entities / features / shared` 레이어를 사용하는 FSD 스타일로 정리되어 있다.
- MVP 범위에서는 마이크로서비스보다 단일 서버 구성이 구현과 운영 모두에 유리하다.
- AI 호출은 서버에서만 수행해야 비용, 보안, 응답 형식 검증을 통제할 수 있다.

## 3. 프론트엔드 구조

모바일 앱은 `apps/mobile`에 위치한다.

```text
apps/mobile
├── app                     # Expo Router 화면과 라우트
└── src
    ├── entities            # 핵심 도메인 모델과 API 타입
    ├── features            # 사용자 액션 중심 기능
    └── shared              # 공통 API 클라이언트, UI, 설정, 테마
```

이 구조는 엄격한 전체 FSD 구현이라기보다, MVP에 맞춘 FSD 스타일 구조로 보는 것이 적절하다.

### 역할 분리 원칙

- `app`: 화면 진입점, 라우팅, 화면 조합
- `entities`: 레시피 같은 핵심 도메인 타입과 도메인 API
- `features`: 재료 선택, 추천 조회, 조리 진행 같은 사용자 기능
- `shared`: 공통 컴포넌트, 색상, 환경변수, HTTP 클라이언트

## 4. 백엔드 구조

현재 저장소의 `apps/api`는 단일 FastAPI 서버 기준으로 구성한다.

```text
apps/api/app
├── api                     # FastAPI 라우터와 의존성 주입
├── core                    # 설정, 보안, 로깅
├── data                    # seed, fixture, 초기 mock 데이터
├── domain                  # Recipe, Ingredient 같은 핵심 모델
├── repositories            # DB 접근 계층
├── schemas                 # 요청/응답 스키마
└── services                # 추천, 표준화, 추가 재료 반영 유스케이스
```

여기서 핵심 원칙은 다음과 같다.

- 앱은 DB에 직접 접근하지 않는다.
- 모든 AI 호출은 API 서버에서만 수행한다.
- 검증된 레시피 데이터를 우선 사용하고 AI는 보조 역할을 맡는다.
- DB 접근은 Repository 계층으로 감싼다.
- FastAPI 진입점은 `app/main.py`에 둔다.

## 5. FSD와 클린 아키텍처의 관계

오늘픽은 두 구조를 한 레이어 체계로 섞는 것이 아니라, 영역별로 다르게 적용하는 편이 맞다.

- 모바일 앱: FSD 스타일
- API 서버: 클린 아키텍처 또는 레이어드 아키텍처

즉, 현재 프로젝트의 올바른 설명은 다음에 가깝다.

```text
모바일은 FSD 스타일로 구성하고
서버는 단일 FastAPI 기반의 레이어드/클린 구조로 분리한다.
```

이 표현이 "FSD와 클린을 합쳤다"는 말보다 정확하다.

## 6. 문서상 최종 판단

현재 저장소 기준으로 가장 일관된 아키텍처 설명은 다음과 같다.

1. 제품은 모바일 앱 중심 서비스다.
2. 앱은 Expo Router 기반 React Native 앱이다.
3. 앱 내부 구조는 FSD 스타일이다.
4. 서버는 단일 FastAPI API 서버를 기준으로 한다.
5. 데이터 저장은 PostgreSQL을 기준으로 한다.
6. AI는 서버 내부 보조 기능으로 사용한다.

## 7. 다음 정리 대상

문서와 저장소를 완전히 맞추려면 다음 작업이 이어져야 한다.

- API 계약서 초안 작성
- 모바일의 mock 데이터와 실제 API 응답 형식 정렬
- 루트 `README.md`에 최종 구조 반영
